import { Routes } from '@angular/router';
import { ListagemCartas } from './listagem-cartas/listagem-cartas';

export const routes: Routes = [
    { path: 'listacartas', component: ListagemCartas },
];
